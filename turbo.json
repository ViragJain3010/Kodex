{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "lint": {
      "cache": true
    },
    "lint:check": {
      "cache": true
    },
    "test": {
      "dependsOn": ["^build", "lint:check"],
      "cache": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**", "out/**"],
      "cache": true
    }
  }
}
